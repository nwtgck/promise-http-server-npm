{
  "name": "promise-http-server-example",
  "version": "1.0.0",
  "description": "Example of promise-http-server",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "babel src -d dist",
    "simple-http-server": "npm run build && node dist/simple-http-server.js"
  },
  "author": "Ryo Ota",
  "dependencies": {
    "promise-http-server": "file:.."
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.7.0"
  }
}
